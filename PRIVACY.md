# 隐私说明

本文说明 Mobile Agent 当前源码版本的数据处理方式。用户自行配置的模型、语音或网络服务有各自的隐私政策，使用前应独立确认。

## 本地保存的数据

应用会在 Android 应用私有目录中保存：

- 对话、消息、工具调用记录和上下文摘要。
- 模型与语音服务配置、个性化设置和界面偏好。
- 用户导入的临时副本、模型图片缓存和工具生成文件。
- 模型服务返回的 Token 用量统计。

模型和语音服务密钥使用 Android Keystore 派生密钥加密后保存。应用不包含开发者统一提供的生产 API Key。

当前应用允许 Android 系统备份。模型设置密文已排除在备份之外，但对话数据库和部分普通设置仍可能按设备与系统策略进入备份；后续版本会继续收紧备份范围。

## 可能发送到外部的数据

只有在相应功能被使用时，应用才会连接外部服务：

- **模型服务**：向用户配置的兼容接口发送对话、附件、工具结果，以及完成当前任务所需的图片或设备界面信息。应用允许连接局域网或自托管的 HTTP 接口；HTTP 传输未加密，可能被同一网络中的其他设备观察或篡改，非可信网络应使用 HTTPS。
- **语音转写**：向用户选择的语音服务发送录音内容和必要的接口参数。
- **网络搜索与网页读取**：向 DuckDuckGo 发送搜索词，并直接访问用户或模型选择的公开网页。
- **系统分享**：在用户选择目标应用后，通过 Android 分享界面交付文字或文件。

项目本身没有广告、行为分析或遥测 SDK。是否产生服务端日志、保留期限以及数据使用方式，由用户配置的外部服务决定。

## 高权限能力

- 麦克风用于用户主动发起的语音输入。
- 悬浮窗用于显示任务状态、授权、停止入口和悬浮对话。
- 通知用于后台任务状态和返回原对话。
- 开机启动仅用于恢复用户已主动启用的常驻悬浮助手。
- 应用列表用于把可启动应用名称映射到设备操作目标。
- 无障碍、截图与 Root 能力用于用户授权的手机操作任务，可能接触当前屏幕内容。

敏感操作应由用户明确授权。应用设计上不应自动操作支付、验证码或账号安全流程，也不向模型开放任意 Root Shell。

## 数据删除

用户可以在应用内删除对话并清理无用缓存和临时文件。卸载应用会删除 Android 应用私有目录中的本地数据；系统备份中的副本由 Android 和用户的备份账户管理。

## 变更

隐私行为发生变化时，应在合并相关功能时同步更新本文档。正式发布版本的 Release 说明会标注重要隐私变化。
